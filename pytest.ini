[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    live: requires the running stack and performs real Hedera testnet payments; opt in with PROOFLINE_LIVE=1
    slow: takes several seconds or longer
